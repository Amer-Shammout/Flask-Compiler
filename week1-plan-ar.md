# خطة الأسبوع الأول (تفصيلية وقابلة للاختبار) — Semantic Analysis

التاريخ: 2026-05-08

**النطاق**: تجهيز البنية الأساسية (AST + Symbol Table + Diagnostics) مع ضمان عمل **Visitors الخاصة بالـ AST فقط**.  
**الفريق**: 5 أشخاص.  
**مخرجات الأسبوع**: بنية جاهزة للتحليل الدلالي بدون أخطاء تشغيلية.

---

## توزيع الأدوار

- **شخص 1 (Integrator):** دمج التعديلات وربط المسار الكامل.
- **شخص 2 (Flask AST):** تحديث عقد Flask AST + المواقع + getters.
- **شخص 3 (Template AST):** تحديث عقد Template AST + المواقع + getters.
- **شخص 4 (Symbol + Diagnostics):** بناء Symbol Table من الصفر + نظام Diagnostics.
- **شخص 5 (AST Visitors):** تحديث FlaskVisitor وTemplateVisitor وProgramVisitor (الموجودين ضمن AST).

---

## أهداف الأسبوع 1

1. AST قابل للتحليل (getters + موقع العقد). 
2. Symbol Table موحّد جديد (نطاقات + رموز). 
3. Diagnostics موحّد (تجميع أخطاء). 
4. AST Visitors تبقى كما هي منطقيا، مع تغذية الحقول الجديدة (المواقع + getters).
5. منع استخدام `toString` لأي استخراج بيانات في مراحل الـ Semantic/Symbol Builder.
6. إزالة أي اعتماد على Symbol Table الحالي (سيُحذف كليًا).

---

## خطة اليوم الأول (مهام متوازية)

### مهمة 1 — Diagnostics (شخص 4)
- **العمل**: إنشاء كائن Diagnostic + مجمّع أخطاء. 
- **اختبار**: تشغيل مسار تحليل فارغ والتأكد من إنشاء تقرير أخطاء فارغ.

### مهمة 2 — تصميم Symbol/Scope (شخص 4)
- **العمل**: إنشاء بنية SymbolKind + ScopeType + عمليات enter/exit/declare/lookup. 
- **اختبار**: اختبار وحدة بسيط (تعريف رمز ثم البحث عنه). 

---

## خطة اليوم الثاني (Flask AST) — مهام متوازية

### مهمة 3 — Flask AST getters (شخص 2)
- **العمل**: إضافة getters صريحة لعقد Flask الأساسية (expr/stmt/suite). 
- **اختبار**: تشغيل ProgramVisitor على عينة Flask والتأكد من عدم وجود استثناءات.

### مهمة 4 — مواقع العقد (شخص 2)
- **العمل**: إضافة line/column أو span لعقد Flask. 
- **اختبار**: طباعة موقع عقدتين ومقارنتهما مع الملف الأصلي.

---

## خطة اليوم الثالث (Template AST) — مهام متوازية

### مهمة 5 — Template AST getters (شخص 3)
- **العمل**: إضافة getters لعقد HTML/CSS/Jinja. 
- **اختبار**: تشغيل TemplateVisitor لعينة HTML والتأكد من الوصول لاسم الوسم وقيمة السمة.

### مهمة 6 — مواقع العقد (شخص 3)
- **العمل**: إضافة line/column أو span لعقد Template. 
- **اختبار**: مقارنة سطر HtmlText أو JinjaExpr مع موقعه الفعلي.

---

## خطة اليوم الرابع (AST Visitors)

### مهمة 7 — FlaskVisitor alignment (شخص 5)
- **العمل**: تحديث FlaskVisitor لملء الحقول الجديدة (المواقع + البيانات التي أضيفت في AST). 
- **اختبار**: بناء AST لعينة Flask ومقارنة عدد العقد مع السابق.

### مهمة 8 — TemplateVisitor alignment (شخص 5)
- **العمل**: تحديث TemplateVisitor لملء المواقع وربط البيانات الجديدة. 
- **اختبار**: تشغيل Parser على عينة Template بدون استثناءات.

---

## خطة اليوم الخامس (تكامل أولي)

### مهمة 9 — Symbol Builder أولي (شخص 4)
- **العمل**: بناء جدول رموز Flask جديد (دوال + متغيرات + معاملات) بدون الاعتماد على Symbol Table الحالي. 
- **اختبار**: طباعة جدول رموز لعينة Flask والتأكد من ظهور الدوال والمتغيرات.

### مهمة 10 — ربط المسار الكامل (شخص 1)
- **العمل**: ربط ProgramVisitor → AST → Symbol Builder → Diagnostics. 
- **اختبار**: تشغيل مسار كامل على ملف Flask سليم والحصول على تقرير أخطاء فارغ.

---

## اختبار إغلاق الأسبوع

- تشغيل عينة Flask وعينة Template عبر المسار الجديد بدون أخطاء تشغيلية. 
- التأكد من أن كل Getter جديد مستخدم فعليا في Visitor واحد على الأقل. 
- التأكد من أن أي استخراج بيانات في Semantic/Symbol Builder لا يعتمد على toString.
- التأكد من أن Symbol Table الجديد يُنتج نتائج قابلة للعرض. 
- التأكد من حذف/عدم استخدام أي ملفات خاصة بالـ Symbol Table القديم.

---

## ملاحظات مرونة الخطة

- إذا تأخر أحد المسارات، يمكن تأجيل جزء من Template AST إلى بداية الأسبوع الثاني.
- يمكن دمج مهمتي 7 و8 إذا كان الفريق يحتاج تسريع التنسيق.
- مهام Flask AST وTemplate AST يمكن تنفيذها بالتوازي بالكامل.

---

نهاية الخطة.

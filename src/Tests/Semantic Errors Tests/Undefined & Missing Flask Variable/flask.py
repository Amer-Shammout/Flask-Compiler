import math

TAX = 0.15

def calculate_total(price):
    return price + tax_rate      #Undefined Variable

def calculate_discount(price):
    return price * DISCOUNT_RATE #Undefined Variable

def print_customer(customer):
    print(customer_name)         #Undefined Variable

def process_order(price):
    total = calculate_total(price)

    final_price = apply_discount(total)    #Undefined Function

    shipping = calculate_shipping(total)   #Undefined Function

    return final_price + shipping

def main():
    products = [
        1000,
        50,
        200,
        75
    ]

    for product in products:
        print(product)

        total = process_order(product)

        print(total)

        print(currency_symbol)   #Undefined Variable

    report = generate_report(products)     #Undefined Function

    print(report)

    print(user.email)           #Undefined Variable

    value = unknown_object.id   #Undefined Variable

    result = missing_func()     #Undefined Function

    average = sum(numbers) / len(numbers)  #Undefined Variable

    print(average)

if __name__ == "__main__":
    main()
